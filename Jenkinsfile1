pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                 def myhome=tool name: 'Maven', type: 'maven'
                 sh "${myhome}/bin/mvn --version"
            }
        }
    }
}
